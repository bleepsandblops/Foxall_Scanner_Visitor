extends ../layout
block content
  .page-header
    h2
      if env == 'exhibition'
        .menu-button-container
          a(href="/scannerwall", class="menu-button wall-header") Publishing Rooms 
          a(href="/scannerwall", class="menu-button wall-header") Publishing Rooms 
          a(href="/scannerwall", class="menu-button wall-header") Publishing Rooms 
      if env == 'public'  
        .menu-button-container
          a(href="/", class="menu-button wall-header") Publishing Rooms 
          a(href="/", class="menu-button wall-header") Publishing Rooms 
          a(href="/", class="menu-button wall-header") Publishing Rooms 
      span() / 
        a(href="/scannerwall", class="") Wall Scan 
      span() / 
        span(class="colour colour--2") Wall Scan Search

  .wall-search-controls
    div.js--day-select
      each day, i in days
        a(href="", data-day=day) #{day}
    div.js--time-select

    a(class="js--timeline-link timelines-link" href="#") go